apiVersion: v1alpha1
discovery:
  jobs:
    - type: AWS/Usage
      regions:
        - us-east-1
      period: 300
      length: 300
      metrics:
        - name: CallCount
          statistics: [Sum]
        - name: ResourceCount
          statistics: [Sum]
